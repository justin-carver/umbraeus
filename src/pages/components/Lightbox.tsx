import { Center, Stack, Group, Text, Image } from '@mantine/core';

const Lightbox = (props: any) => {
	return (
		<Stack>
			<Image src="" alt="" />
			<Text></Text>
		</Stack>
	);
};

export default Lightbox;
